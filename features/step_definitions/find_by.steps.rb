
When /^searching the log for the application '([^']+)'$/ do |name|
  pending
end

When /^searching the log for the log severity (\w+)$/ do |severity|
  pending 
end

When /^searching the log for the log severity (\w+) and (higher|lower)$/ do |severity,direction|
  pending
end

When /^searching the log for the exact match of the message "([^"]+)"$/ do |severity|
  pending
end

When /^searching the log for matches of the message "([^"]+)"$/ do |severity|
  pending
end

When /^searching the log for the time (.+)$/ do |time|
  pending
end

Then /^I expect to have found (no|\d+) log entr(?:y|ies)$/ do |count|
  pending
end

Then /^I expect the (first|last|\d+(?:st|nd|rd|th)) entry to be:$/ do |position,expected|
  pending
end